// Write a program that prints only even numbers between 1 and 10 using a for loop
for (let evenNum = 2; evenNum <= 10; evenNum += 2) {
  // Log the even numbers to the console
  console.log(evenNum);
}
